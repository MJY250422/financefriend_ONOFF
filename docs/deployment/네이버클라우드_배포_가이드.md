# 🇰🇷 네이버 클라우드 플랫폼(NCP) 배포 가이드

**작성일**: 2025.11.05  
**목적**: 네이버 클라우드를 사용하여 데이터베이스와 백엔드 서버 배포

---

## 📊 네이버 클라우드 vs 현재 구성 비교

| 구성 요소 | 현재 (Supabase + 로컬) | 네이버 클라우드 |
|---------|---------------------|---------------|
| **데이터베이스** | Supabase PostgreSQL | Cloud DB for PostgreSQL |
| **백엔드 서버** | 로컬 (localhost:8000) | Server (VPC) 또는 Cloud Functions |
| **가격** | Supabase 무료 플랜 | 무료 크레딧 + 사용량 과금 |
| **지원 언어** | 영어 | 한국어 ⭐ |
| **데이터센터** | 싱가포르/미국 | 한국 (빠른 속도) ⭐ |
| **학습 곡선** | 쉬움 | 중간 |

---

## ✅ 네이버 클라우드 장점

### 1. **한국어 지원**
- 한국어 콘솔 및 문서
- 한국어 고객 지원

### 2. **빠른 속도**
- 국내 데이터센터 (서울, 부산)
- 낮은 레이턴시

### 3. **무료 크레딧**
- 신규 가입 시 100,000원 크레딧 제공
- 학생/스타트업 추가 크레딧

### 4. **통합 플랫폼**
- 데이터베이스, 서버, 스토리지 한 곳에서 관리
- 네이버 생태계 통합 (Papago, Clova 등)

---

## 🚀 네이버 클라우드 배포 방법

### 옵션 1: Cloud DB for PostgreSQL + Server (VPC) ⭐ 권장

**구성:**
```
[네이버 클라우드]
├─ Cloud DB for PostgreSQL (데이터베이스)
└─ Server (VPC) (백엔드 FastAPI 서버)
```

**장점:**
- 안정적인 프로덕션 환경
- 완전한 제어 가능
- 확장성 좋음

**단점:**
- 초기 설정 복잡 (1-2시간)
- 비용 발생 (무료 크레딧 사용 가능)

---

### 옵션 2: Cloud DB for PostgreSQL + Render/Railway (하이브리드)

**구성:**
```
[네이버 클라우드]
└─ Cloud DB for PostgreSQL (데이터베이스)

[Render/Railway]
└─ 백엔드 FastAPI 서버
```

**장점:**
- 데이터베이스는 한국에 (빠른 속도)
- 백엔드는 쉽게 배포 (Render/Railway)

**단점:**
- 두 플랫폼 관리 필요
- 네트워크 레이턴시 약간 증가

---

## 📋 옵션 1 상세 가이드: NCP 완전 배포

### Step 1: 네이버 클라우드 플랫폼 가입

1. **회원가입**
   - https://www.ncloud.com
   - 신규 가입 시 100,000원 크레딧 제공

2. **결제 수단 등록**
   - 신용카드 또는 체크카드
   - (무료 크레딧 범위 내에서 사용하면 과금 없음)

---

### Step 2: Cloud DB for PostgreSQL 생성

1. **콘솔 접속**
   - https://console.ncloud.com

2. **Services → Database → Cloud DB for PostgreSQL**

3. **DB 서버 생성**
   ```
   서버 이름: financefriend-db
   DB 버전: PostgreSQL 14
   서버 타입: Standard (2vCore, 4GB)
   스토리지: 10GB SSD
   DB User: postgres
   DB Password: [안전한 비밀번호 설정]
   ```

4. **네트워크 설정**
   - VPC 생성 또는 기존 VPC 선택
   - 서브넷 설정

5. **ACG (Access Control Group) 설정**
   ```
   프로토콜: PostgreSQL
   접근 소스: 0.0.0.0/0 (전체 허용, 나중에 제한)
   포트: 5432
   ```

6. **DB 생성 완료**
   - 접속 정보 확인
   ```
   호스트: xxx.kr-seoul-1.ncloud.com
   포트: 5432
   데이터베이스: postgres
   사용자: postgres
   비밀번호: [설정한 비밀번호]
   ```

7. **연결 문자열**
   ```
   postgresql://postgres:[PASSWORD]@xxx.kr-seoul-1.ncloud.com:5432/postgres
   ```

---

### Step 3: Server (VPC) 생성 - 백엔드 서버

1. **Server → Server 생성**

2. **서버 설정**
   ```
   서버 이름: financefriend-backend
   서버 이미지: Ubuntu 22.04
   서버 타입: Standard (2vCore, 4GB)
   스토리지: 50GB
   ```

3. **네트워크 설정**
   - 데이터베이스와 같은 VPC 선택
   - 공인 IP 자동 할당 체크

4. **ACG 설정**
   ```
   프로토콜: TCP
   포트: 8000 (FastAPI)
   접근 소스: 0.0.0.0/0
   
   프로토콜: TCP
   포트: 22 (SSH)
   접근 소스: My IP
   ```

5. **서버 생성 완료**
   - 공인 IP 확인 (예: 123.45.67.89)
   - 관리자 비밀번호 확인

---

### Step 4: 백엔드 서버 배포

1. **SSH 접속**
   ```bash
   ssh root@123.45.67.89
   ```

2. **Python 3.11 설치**
   ```bash
   sudo apt update
   sudo apt install -y software-properties-common
   sudo add-apt-repository ppa:deadsnakes/ppa
   sudo apt update
   sudo apt install -y python3.11 python3.11-venv python3.11-dev
   sudo apt install -y git nginx
   ```

3. **프로젝트 클론**
   ```bash
   git clone https://github.com/MJY250422/financefriend_ONOFF.git
   cd financefriend_ONOFF
   git checkout feature/connect_streamlit_fastapi
   ```

4. **가상환경 설정**
   ```bash
   cd system_design
   python3.11 -m venv venv311
   source venv311/bin/activate
   pip install -r requirements.txt
   ```

5. **.env 파일 생성**
   ```bash
   nano .env
   ```
   
   내용:
   ```env
   # NCP Cloud DB PostgreSQL
   DATABASE_URL=postgresql://postgres:[PASSWORD]@xxx.kr-seoul-1.ncloud.com:5432/postgres
   
   API_HOST=0.0.0.0
   API_PORT=8000
   API_RELOAD=False
   
   ALLOWED_ORIGINS=*
   LOG_LEVEL=INFO
   ```

6. **서버 실행 테스트**
   ```bash
   python main.py
   ```

7. **systemd 서비스 등록 (자동 시작)**
   ```bash
   sudo nano /etc/systemd/system/financefriend.service
   ```
   
   내용:
   ```ini
   [Unit]
   Description=FinanceFriend FastAPI Server
   After=network.target
   
   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/financefriend_ONOFF/system_design
   Environment="PATH=/root/financefriend_ONOFF/system_design/venv311/bin"
   ExecStart=/root/financefriend_ONOFF/system_design/venv311/bin/python main.py
   Restart=always
   
   [Install]
   WantedBy=multi-user.target
   ```
   
   활성화:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl start financefriend
   sudo systemctl enable financefriend
   sudo systemctl status financefriend
   ```

8. **Nginx 리버스 프록시 설정 (선택)**
   ```bash
   sudo nano /etc/nginx/sites-available/financefriend
   ```
   
   내용:
   ```nginx
   server {
       listen 80;
       server_name 123.45.67.89;
   
       location / {
           proxy_pass http://127.0.0.1:8000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
   }
   ```
   
   활성화:
   ```bash
   sudo ln -s /etc/nginx/sites-available/financefriend /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

---

### Step 5: 팀원들에게 공유

**백엔드 API URL:**
```
http://123.45.67.89:8000
또는
http://123.45.67.89 (Nginx 사용 시)
```

**팀원들 설정:**

`streamlit/api_client.py` 수정:
```python
BASE_URL = "http://123.45.67.89:8000"
```

---

## 💰 비용 예상

### Cloud DB for PostgreSQL
- **Standard (2vCore, 4GB, 10GB SSD)**
- 월 약 30,000원
- 무료 크레딧 사용 시 3개월 무료

### Server (VPC)
- **Standard (2vCore, 4GB, 50GB)**
- 월 약 25,000원
- 무료 크레딧 사용 시 4개월 무료

### 총 비용
- 월 약 55,000원
- **무료 크레딧 100,000원으로 약 2개월 무료 사용 가능!**

---

## 🆚 비교: 네이버 클라우드 vs Render vs Supabase

| 항목 | 네이버 클라우드 | Render + Supabase | ngrok + Supabase |
|-----|---------------|-------------------|------------------|
| **DB 위치** | 한국 (빠름) | 싱가포르 | 싱가포르 |
| **서버 위치** | 한국 (빠름) | 미국/유럽 | 로컬 PC |
| **속도** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **안정성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **가격** | ~55,000원/월 | 무료 | 무료 |
| **무료 기간** | 2개월 (크레딧) | 무제한 | 제한적 |
| **설정 난이도** | 중간~어려움 | 쉬움 | 매우 쉬움 |
| **한국어 지원** | ⭐⭐⭐⭐⭐ | ❌ | ❌ |

---

## 🎯 추천 시나리오

### 시나리오 1: 빠른 테스트 (1주일)
```
ngrok + Supabase
- 가장 빠르게 시작 (5분)
- 팀원들과 기능 테스트
```

### 시나리오 2: 프로젝트 개발 (1-3개월)
```
Render + Supabase
- 무료로 안정적 운영
- 개발 및 테스트
- 무료 크레딧 없어도 OK
```

### 시나리오 3: 실제 서비스 런칭 (장기)
```
네이버 클라우드 (전체)
- 빠른 속도 (한국 서버)
- 프로덕션 레벨 안정성
- 학교 프로젝트 또는 스타트업
```

### 시나리오 4: 하이브리드 (추천) ⭐
```
Render (백엔드) + Supabase (DB)
↓ 프로젝트 성장 후
네이버 클라우드로 마이그레이션
```

---

## ✅ 다음 작업 추천 순서

### 이번 주: 빠른 시작
1. **Render 배포** (30분)
   - 무료, 쉬움, 안정적
   - GitHub 자동 배포

2. **Supabase 유지**
   - 이미 설정 완료
   - 무료 플랜 충분

### 나중에: 네이버 클라우드 고려
- 프로젝트가 성장하면
- 사용자가 많아지면
- 빠른 속도가 필요하면

---

## 📞 네이버 클라우드 학습 자료

1. **공식 문서**
   - https://guide.ncloud-docs.com

2. **무료 교육**
   - https://www.ncloud.com/support/education

3. **학생/스타트업 크레딧**
   - https://www.ncloud.com/product/credits

---

## 🎯 결론

### 지금 당장 추천
**Render + Supabase 조합 사용**
- 이유: 무료, 쉬움, 팀 협업에 충분

### 나중에 고려
**네이버 클라우드 전환**
- 이유: 빠른 속도, 한국어 지원, 프로덕션 레벨

---

## 💡 제 추천

**단계별 접근:**

```
1단계 (이번 주): Render로 배포
   ↓ 팀원들과 개발 시작
   
2단계 (개발 중): 기능 구현
   ↓ 프로젝트 완성도 높이기
   
3단계 (런칭 전): 네이버 클라우드 검토
   ↓ 필요시 마이그레이션
   
4단계 (런칭): 프로덕션 배포
```

**이렇게 하면:**
- 지금 빠르게 시작 가능
- 개발하면서 배움
- 나중에 더 나은 선택 가능

---

**네이버 클라우드는 좋은 선택이지만, 지금은 Render로 빠르게 시작하는 게 더 좋습니다!** 🚀

**작성일**: 2025.11.05

