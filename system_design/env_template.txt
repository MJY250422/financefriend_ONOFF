# FinanceFriend Backend Environment Variables
# 이 파일을 복사해서 .env 파일을 만드세요: cp env_template.txt .env

# ===========================================
# Database Configuration
# ===========================================

# Supabase PostgreSQL (프로덕션/협업용)
# Supabase 대시보드 → Settings → Database → Connection String (URI) 에서 확인
# 실제 비밀번호와 프로젝트 참조로 교체하세요
DATABASE_URL=postgresql://postgres:[YOUR_PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# 로컬 PostgreSQL (로컬 개발용, 선택)
# DATABASE_URL=postgresql://financefriend_user:your_password@localhost:5432/financefriend

# SQLite (간단한 테스트용, 선택)
# DATABASE_URL=sqlite:///./financefriend.db

# ===========================================
# API Server Configuration
# ===========================================

# API 서버 호스트 (0.0.0.0 = 모든 네트워크 인터페이스)
API_HOST=0.0.0.0

# API 서버 포트
API_PORT=8000

# 개발 모드 (코드 변경 시 자동 재시작)
API_RELOAD=True

# ===========================================
# CORS Configuration
# ===========================================

# 허용할 Origin (쉼표로 구분)
# Streamlit 앱, 프론트엔드 등
ALLOWED_ORIGINS=http://localhost:8501,http://localhost:3000

# 팀원 추가 시 (예시)
# ALLOWED_ORIGINS=http://localhost:8501,http://192.168.0.10:8501,http://팀원IP:8501

# 개발 중 모든 Origin 허용 (보안 주의!)
# ALLOWED_ORIGINS=*

# ===========================================
# Optional: AI/ML Services (추후 사용)
# ===========================================

# OpenAI API Key (선택)
# OPENAI_API_KEY=sk-...

# Other API Keys
# NEWS_API_KEY=...

# ===========================================
# Optional: Security (추후 사용)
# ===========================================

# JWT Secret Key (선택)
# JWT_SECRET_KEY=your-secret-key-here
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_MINUTES=1440

